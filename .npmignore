# Scratch / POC files (not part of the published package)
chat-mirror-poc.ts
terminal-poc.ts
tail-interactive-jsonl.ts
verify-billing-signature.ts
codex-appserver-spike.ts
codex-cli-poc.ts
codex-sdk-poc.ts
codex-junction-skill-probe.ts
.out.txt

# Dev files
.claude/
.github/
.vite/
.env
.env.test
.DS_Store
.playwright-mcp
.cursor/
plans/
*.png
sdh-uit-edu-vn-screenshot.png
tests/
repomix-output.xml
ppm.yaml
*.pid
*.log

# Compiled binaries + release artifacts — too large for npm (users run via bun
# directly). Only dist/web/ (the prebuilt frontend) ships. Leftovers from a prior
# `scripts/release.sh` run must never leak into the tarball (ERR_STRING_TOO_LONG).
dist/ppm
dist/ppm-*
dist/ppm.exe
dist/.cli-cache/
dist/*.tar.gz
dist/*.zip
dist/SHA256SUMS

# Vite may leak absolute paths into src/web/
src/web/Users/

# Keep dist/web/ for pre-built frontend

# Local design/reference assets
.support-intl.png
marketing/
